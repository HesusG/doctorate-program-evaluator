# Configuración de la base de datos
MONGODB_URI=mongodb://localhost:27017/doctorados

# API Key de OpenAI (requerida para el enriquecimiento de datos)
OPENAI_API_KEY=sk-tu-api-key-aqui

# Secreto para la autenticación JWT
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=jwt_secreto_de_ejemplo_cambiame_en_produccion

# Puerto para el servidor (opcional, por defecto es 3000)
PORT=3000